postgres:
    config:
        data:
            - key: POSTGRES_DB
              value: postgres
            - key: POSTGRES_USER
              value: postgres
            - key: POSTGRES_PASSWORD
              value: postgres